# core HTTP + socks support
requests>=2.28.0
PySocks>=1.7.1

# YAML parsing
PyYAML>=6.0

# HTML parsing (used for link extraction)
beautifulsoup4>=4.12.0

# optional: programmatic Tor control (NEWNYM)
stem>=1.9.0
